{
  "buildCommand": "cd .. && npm install && npm run build:data-provider && npm run build:mcp && npm run build:data-schemas && cd client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
